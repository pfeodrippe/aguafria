;; CIDER adds its own nREPL middleware; :dev supplies the native JVM options.
;; For checkout development, change the alias string to ":dev:local-aguafria".
((nil . ((cider-clojure-cli-aliases . ":dev"))))
